import{ Component }from "@angular/core"

@Component({
    // Seletor  - > o nome da tag HTML que vamos chamar no app 
    selector: 'app-olamundo', 
    //Template -> é o html que será utilizado
    template: '<h1>Adeus Mundo!</h1>',

})
//Quando quiser importar uma class no app precisamos usar esse nome. 
export class olamundoComponent{

}